import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class regptest {
    public static void main(String[] args) {

        String str = "12}";
        System.out.println(str.length() + "" +str.substring(0,2));
    }
}
